//Check foreach loop variable having a similar name

shader ComputeColor
{
	int Compute(int i)
	{
		return i;
	}
}

shader ComputeColorA : ComputeColor
{
	override int Compute(int i)
	{
		return -i;
	}
}

shader ShaderA
{
	compose ComputeColor comps1[];
	compose ComputeColor comps2[];
	
	int main()
	{
		int res = 0;
		foreach(var subCompute in comps1)
		{	
			foreach(var subCompute in comps2)
			{
				res += subCompute.Compute(res);
			}
		}
		
		return res;
	}
}

