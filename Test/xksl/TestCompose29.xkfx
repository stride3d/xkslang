convertAndLoadRecursif "TestCompose29" TestShaderMain

mixer m
m.mixin( TestShaderMain )
m.addComposition( directLightGroups = [mixin(TestDirectLightGroup), mixin(TestDirectLightGroup)] )
m.addComposition( materialPixelStage = mixin(TestMaterialSurfaceArray) )

m.setStageEntryPoint( Vertex="VSMain" )
m.compile()
