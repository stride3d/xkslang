shader ShaderMain<int G1, int G2> : ShaderBase<G2, (-G1 + G2 * 2)>
{
	stage stream int aStream: ASTREAM;
	
	int compute()
	{
		return base.compute();
	}
	
	stage void VSMain() 
    {
		streams.aStream = compute() + (G1 + G2);
    }
}
