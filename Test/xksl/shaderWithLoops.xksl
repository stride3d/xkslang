shader Shader
{
	int ComputeFor(int count)
	{
		int res = 0;
		for (int i=0; i<count; ++i)
		{
			res++;
		}
		return res;
	}
	
	int ComputeWhile(int count)
	{
		int res = 0;
		while (count > 0)
		{
			res += count;
			count--;
		}
		return res;
	} 
}
