shader ShaderMain
{
	stage stream float matBlend: MATBLEND;

	float Compute(Streams fromStream)
	{
		return fromStream.matBlend;
	}
	
	void VSMain()
	{
		Streams backup = streams;
		float f = Compute(backup);
		//float f2 = Compute(streams);
	}
}
