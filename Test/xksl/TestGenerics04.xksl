
shader ShaderBase<float offset>
{
	float compute()
	{
		return offset;
	}
}

shader ShaderMain<int TapCount, float aFloat> : ShaderBase<aFloat>
{
	int array[TapCount];
	
	stage int VSMain() 
    {
		return compute() + array[TapCount - 1];
    }
}
