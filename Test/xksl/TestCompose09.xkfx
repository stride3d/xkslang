convertAndLoad "TestCompose09.xksl"

mixer A
A.mixin( SubComp )

mixer B
B.mixin( Comp )
B.addComposition( Comp.sb = A )

mixer m
m.mixin( ShaderA )
m.addComposition( ShaderA.color = B )
m.addComposition( ShaderA.sb = A )

m.setStageEntryPoint( Pixel="main" )
m.compile
