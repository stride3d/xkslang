
shader ShaderMain<MemberName TRgba1, MemberName TRgba2> : ShaderBase<TRgba2>
{
	stage stream float4 LocalColor : TStream;

    override float4 Compute() {
        return streams.LocalColor.TRgba1 + base.Compute();
    }
	
	stage void VSMain() 
    {
		float4 color = Compute();
    }
}
